pipeline {
    agent { label "agent1" }
    stages {
        stage("build") {
            steps {
                sh '''
                    docker --version
                    docker-compose -v
                    '''         
            }
        }
    }
} 